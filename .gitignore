# raw telemetry stays out of the repo (lives in Azure Blob)
data/
*.csv
__pycache__/
.venv/
local.settings.json

# Security findings stay local - this repo is public.
.local/

# Demo build output (ADR-012). The fixtures under
# dashboard/public/demo-api ARE committed - they are generated by
# scripts/generate_demo_data.py and reviewed as a diff - but the
# compiled demo bundle is not.
dashboard/dist-demo/
