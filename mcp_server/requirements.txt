# mcp 2.0.0 dropped mcp.server.fastmcp (renamed/restructured to
# mcp.server.mcpserver.MCPServer) - pin below that until server.py is
# migrated off FastMCP.
mcp<2.0.0
azure-identity
azure-storage-blob
python-tds
# pytds's TLS hostname check still calls X509.get_extension(), removed in
# pyOpenSSL 26.2 - pin below that until pytds updates to pyca/cryptography.
pyOpenSSL<26.2
certifi
# floor, not a full pin: GHSA-537c-gmf6-5ccf (statically-linked vulnerable
# OpenSSL in the wheel, CVSS 7.5) is fixed in 48.0.1 - found via pip-audit
# during the 2026-08-03 security review (docs/specs/security-review.md).
cryptography>=48.0.1
