# mcp 2.0.0 dropped mcp.server.fastmcp (renamed/restructured to
# mcp.server.mcpserver.MCPServer) - pin below that until server.py is
# migrated off FastMCP.
mcp<2.0.0
azure-identity
azure-storage-blob
# Bearer-token validation for the OAuth Resource Server (mcp_server/auth.py):
# JWKS signature verification against the CIAM tenant. Same dependency
# ingest/api_auth.py already uses for the dashboard's read API.
PyJWT[crypto]
python-tds
# pytds's TLS hostname check used to call X509.get_extension(), removed
# in pyOpenSSL 26.2 - ingest/_pytds_tls_compat.py monkeypatches that
# check to use `cryptography` directly instead, so pyOpenSSL/cryptography
# are free to float and pick up the GHSA-537c-gmf6-5ccf fix
# (cryptography>=48.0.1). See docs/specs/security-review.md finding #3
# for the live-incident this pin previously caused.
pyOpenSSL>=26.2
cryptography>=48.0.1
certifi
